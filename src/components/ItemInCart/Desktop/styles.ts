import { styled } from 'styled-components';
export const Div=styled.div`
    display: flex;

    img{
        width:10rem;
        object-fit: cover;
    }

    >div{
        display: inherit;
    }
`